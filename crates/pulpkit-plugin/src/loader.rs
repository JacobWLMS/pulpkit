/// Plugin loader — loads .so files via dlopen
/// Stub for Plan 1: built-in plugins will be linked directly
pub struct PluginLoader;

impl PluginLoader {
    pub fn new() -> Self {
        Self
    }
}
